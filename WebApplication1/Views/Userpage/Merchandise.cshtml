@using Microsoft.AspNetCore.Identity
@using Animerch.Data;

@model IEnumerable<Animerch.Models.Transaction>

@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

@{
    ViewData["Title"] = "Merchandise";
}

@if (SignInManager.IsSignedIn(User))
{
    <h2>My Merchandise</h2>

    
    <p>
        <a asp-controller="Merchandise" asp-action="Index">Add new merchandise</a>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Merchandise.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Merchandise.Series)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Merchandise.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Merchandise.Manufacturer)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Amount)
                </th>
                <th>
                    @*Empty <table head> to fix layout over edit and delete column*@
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Merchandise.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Merchandise.Series)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Merchandise.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Merchandise.Manufacturer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Amount)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                </td>

            </tr>
            }
        </tbody>
    </table>
}
else
{ 
    <h2>You must be logged in to see this page</h2>
}
